# AB招新系统
## 项目结构
```
ab_project
    |————docs       (swagger自动生成目录，api文档)
    |————global     (全局配置信息存放处)
    |————middle     (中间件存放处)
    |————model      (用户结构体等存放位置)
    |————mysqlDB    (数据库层)
    |————router     (路由管理层)
    |————service    (核心服务层)
         └──response(出参结构体)
```
